# 0911 Walk1 FDE forensic amendment

Status: DONE / ENGINEERING_PASS / WALK1_GATE_FAILED. Authorization: user supplied “Walk1 FDE 零召回取证、最小修复与固定场景验收” implementation plan (2026-09-11). Baseline HEAD c254c6acdce48a1be66febaaa935e0f2846e9992. This task supersedes the previous completed-task stop boundary.

Evidence root: /home/mint/ws_fusion_uwb/res/fde_forensics_20260911_01; baseline_hashes.json, baseline.tar.gz and baseline_ldd.txt seal source/configuration/binary/dependencies before edits. Protected untracked doc/v2/ie_0911/ and historical artifacts remain unchanged.

Execute original-algorithm forensics, seal A–H diagnosis, select exactly one evidence-supported repair, engineering checks, identical Walk1 normal clean/injected acceptance, then conditional matrix. Use original manifest/truth IDs: link 27956:20276, closed [1664959678.3077347,1664959686.3077347], +0.5 m; do not reselect. Expected 111 affected raw / 30 planned is an assertion to verify, not new evidence.

Independent forensic C++ executable reuses the loader, plan, physical graph, initialization and checked LM. Only this executable may read truth-derived hold-out IDs. Production has no truth argument. Python only pairs/reports. All 111 rows retained; unplanned fields unavailable with NOT_PLANNED. Physical measurements, noise, keys, row mapping, pre/post residuals and all planned tests exported. Verify IMU/non-target/identity/initial/noise invariance and delta prefit=-0.5 at scale-aware machine precision.

Use complete Gaussian whitened A, P=I-AA+, Qrii=sigma_i² Pii, sparse QR/projection (no full dense inverse). Window T=eWᵀ PWW+ eW, rank-based DoF, full submatrix, spectral and projector checks. Report raw and projected nonlinear residual separately. New mathematical utility uses reliable chi-square quantile p=.99; legacy point threshold stays 6.6349. No damping/jitter/prior/tolerance changes.

Truth-only leave-interval/link-out removes just affected planned range factors. Verify identical clean/injected subset and solve once from common original initial Values. Export same-frame full/subset trajectories, held residuals, prediction covariance and correlated solution separation. Masking requires valid subset, significant held evidence and separation consistent with attenuation; residual growth alone is insufficient. G remains INCONCLUSIVE absent power/redundancy evidence.

Repair priority: A/B injection/metadata/plumbing/sign; E whitening/rows/projector/scaling with Gaussian tests then rediagnose; actual temporal semantic violation only. Otherwise independently confirmed D takes priority over C. D: full Gaussian global p=.99 gate; no isolation if consistent; stable single-link-out enumeration, at most one solve/link, original initial/solver, same tangent correlated D_h=K_h S_h-K0, Cdelta=D_h D_hᵀ; subset observable/converged/consistent, significant separation and positive held excess required, unique qualified link only, ambiguity otherwise. Maximal original-gap groups inside link use full held covariance and GLS sign then negative residuals feed original temporal logic. Rank/solver/numerical failures explicit. C only without confirmed masking and with aggregate evidence: truth-free maximal same-link gap groups, original count/duration, full covariance test and GLS sign, unique positive link else ambiguity. New provider/version/statistical/group identity invalidates old caches. Branch formula/selection must be appended here before production edits; never choose by localization gain.

Freeze Stage2, Rc/local sigma, recovery, fixed factors, kappa=2, optimizer, evaluator, scientific configuration and default/legacy. 1800 s per estimator process tree, no algorithm retry. Build, affected regressions, full CTest; physical/identity/not-planned, heteroscedastic/correlated/rank/degenerate/boundary, aggregate-not-diagonal-sum, correlated subset/isolation/global/solver, temporal/empty/suppress, production access tracing, recovery hash audits.

Fresh Walk1 clean/injected six methods and shared producer; report point/support/delay/localization and all Stage2/Rc/LCB/final/fallback data, execute four finals for nonempty support. Matrix gate: clean complete localization <1m and zero retained segments (report point FP/FPR); injected new-bias TP and overlapping support with interpretable evidence, Stage2/dependent finals completed. LCB superiority is not required. Gate failure/global consistency/insufficient evidence stops expansion: NOT_RUN_WALK1_GATE_FAILED. Only after pass run original ten admitted scenarios with original two injected skips, 12/72 layout; reuse new Walk1 runs.

Deliver evidence/fde_forensics/ CSV/JSON/diagnosis plus FDE_FORENSIC_AND_FIX_RESULT.md, commands/exits/hashes/failures/cache identity and explicit NOT_RUN. Update STATUS and CLAIM_EVIDENCE, preserve T10=C2-C/T11=C/C1–C3. No commit/push, tuning, extra hypotheses or next phase.

## Sealed branch selection before production edits

Selected C; [sealed A–H diagnosis](../ie_0911/evidence/fde_forensics/FDE_FORENSIC_DIAGNOSIS.md). Original shared subset solve failed at 100 iterations, so D is INCONCLUSIVE. Window aggregate injected T=52.326346>50.892181 at rank30 (clean4.332634); physical injection/sign and normalization audits passed. No D/global/isolation implementation.

Opt-in `nlos.fde_grouped_test: true`, default false preserves V2. New provider `imu_aided_grouped_fde_v3` / identity V3. Build truth-free maximal planned same-link groups, split only strict gap>Tgap, require original count/duration. For e=r/sigma, covariance Pgg, T=eᵀPgg+e with rank DoF, p=.99. With d_i=1/sigma_i, GLS residual contrast a=(dᵀPgg+e)/(dᵀPgg+d); positive excess iff a<0. Exactly one link with significant positive-excess eligible group may contribute; multiple links => ambiguity and empty support. Negative residual observations in qualifying groups feed original temporal logic; pointwise classification remains separately reported. Full covariance, ranks, tests, GLS sign and counts exported. Nonfinite/rank uncertainty returns failure/unavailable, no fallback to diagonal. New scientific identity binds grouped rule and statistical version; legacy/new cache rejected in both directions. Only provider allowlist extensions in final empty-reference reuse are permitted interface changes; recovery numerical logic remains byte-for-byte frozen.

## Completion and stopping decision

Full build and CTest 31/31 passed. Original subset failed once at the unchanged 100-iteration limit; D remains INCONCLUSIVE. The sealed C branch was implemented without scientific tuning. Both Walk1 producers and all twelve six-method finals completed. Clean <1m/zero-segment gate passed; injected has zero added-component TP and zero support, so expanded matrix is NOT_RUN_WALK1_GATE_FAILED. Both-direction V2/V3 cache rejection, production truth-inaccessible access tracing, same-final-graph/Values and frozen recovery/dependency audits passed. No commits/push or additional scenarios. [Final 18-item report](../ie_0911/FDE_FORENSIC_AND_FIX_RESULT.md).
