Production: raw cache → A17Graph initialization → AutomaticSupportProvider → frozen partition → SegmentRefitter → unconditional ScoreRefitRecoverability → frozen five decisions → final engine/one fallback → independent evaluator. Empty partition dispatch already selects no-C certified policy but score still rejects it. Metadata combines key/beta/bitwise residual predicates; investigate once, no solver changes. Aggregation reads absent CSV as empty, conflating unknown with zero. All changes remain in this work package.
