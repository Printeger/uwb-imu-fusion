# 0911 NLOS injection experiment amendment

Status: DONE / EXECUTED_ZERO_RECOVERY_SUPPORT. User-authorized implementation of the supplied frozen-algorithm plan. Baseline: `46d37f6709aeedb4958e484683fd51e5fd8ec0cc`.

Semi-synthetic persistent NLOS-on-real-data, INJECTED_COMPONENT_ONLY; natural total bias UNKNOWN. Walk1/2/3 full original crops; B=0.5 m, duration target=8 s, seed=911; closed interval. Normal/low × clean/injected, six methods all_range, robust_cauchy, suppress_all, structured_debias, lcb_fixed_full, lcb_partial; one shared Stage2 producer per scenario. At most 12 scenarios / 72 method results. No algorithm, factors, science parameters, stopping rules, defaults or dependencies change.

Selection is measurement-only: clean raw reference and full original FDE, no Stage2/recovery/GT. Exclude any link with persistent two-sided faults under original gap/count/duration rules. Windows start at planned observations strictly after first five initialization keyframes; gap <= original T_gap, endpoint coverage <= T_gap, count >= max(n_min,ceil(D/T_gap)+1), observed duration >= T_min. Prefer exact 8 s globally; otherwise global longest qualified shorter window. Links ordered SHA256(version|911|dataset_id|tag_id|anchor_id), earliest qualified window. Reject any window whose legal raw target measurements would become invalid. Version=NLOS_INJECTION_V1.

Low redundancy: enumerate actual anchor subsets containing selected anchor, 3 then 4 anchors, within size SHA256(version|911|dataset_id|sorted comma-separated anchor IDs). First subset passing clean raw/full FDE and identical selected window/initialization eligibility is locked. No GT or recovery ranking. No subset: skipped. Independently evaluate locked clean raw/Cauchy, full planned output and both aligned RMSE <1 m (20 ms nearest, scale=1 SE3). Failure skips corresponding injected scenario without reselection; selected clean remaining methods still run.

Measurement-only v2 envelope preserves recording and source obs ordinals, binds opaque specification/subset transform hash and payload hashes through existing preparation/Stage2/final identity. No recipe/truth/link/window/amplitude/affected IDs in estimator config/cache. Keep old T07 schema and half-open defaults. Truth sidecars include all affected and planned affected IDs, source/crop/subset/spec hashes and validity audit. Clean/injected keyframes, nominal sigmas and pre-raw-LM Values must match.

Each estimator process tree has 1800 s wall limit; no algorithm retries or tuning. Producer failure => PARENT_CACHE_UNAVAILABLE. Independent evaluation after artifact sealing; complete tested/planned denominators, missing values unavailable, positive excess is main detection, offline sensor-time detection delay, support obs/time overlap, mixed-segment weighted injected truth, Rc/local sigma/LCB formula and same-set audit, proposed/final/fallback separate. Correction tolerance 1e-9 m; bins zero/partial(0,.45)/near-full[.45,.5]/over-full. Full and same-window aligned localization share full alignment. Range errors unavailable without original complete provenance; never substitute post-fit residual/aligned GT.

Before matrix: build, new tests, affected regressions and full CTest; injection no-op/conservation/endpoints/identity/replay, initialization/noise, shared upstream, truth inaccessible with access tracing, metric edge cases. Deliver machine Table A/B and JSON/CSV, command/exit/log ledger, selected manifest and all failures. No statistical significance from three recordings; separately judge LCB vs suppression and low-redundancy amplification, including negative or insufficient evidence. T10=C2-C, T11=C, C1–C3 restrictions remain. No sensitivity/new algorithm/new phase/commit/push. Protected doc/v2 and historical results unchanged.

Evidence root: `/home/mint/ws_fusion_uwb/res/nlos_injection_20260911_01`; pre-edit source/config/binary/dependency snapshot: baseline_hashes.json and baseline.tar.gz.

## User-authorized pause/resume

User requested a low-battery pause and then explicitly requested continuation. Seven E2E scenarios were complete; Walk3 normal-injected producer was terminated by user pause, with its raw/Cauchy already complete. Resume restarts only this interrupted producer from unchanged raw input in a new isolated batch, plus its four previously unrun finals, and the two unstarted Walk3 low E2E batches. Preserve interrupted attempt and all algorithm failures; no completed estimator is repeated, no failure-driven retry/tuning, no checkpoint/warm start. Resume ledger: RESUME.json, old pause ledger remains PAUSED.json.

Final evidence: [machine-generated result](../ie_0911/NLOS_INJECTION_EXPERIMENT_RESULT.md), report/results.json and engineering/final_results_audit.json. All ten executed scenarios have empty temporal support; no injected true positives, no demonstrated recovery benefit. No claim upgrade.
