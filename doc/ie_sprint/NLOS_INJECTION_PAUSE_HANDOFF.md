> 已恢复并完成：见 `RESUME.json`（COMPLETE）与 [最终结果](../ie_0911/NLOS_INJECTION_EXPERIMENT_RESULT.md)。以下保留历史暂停记录，不再代表当前待办。

# NLOS injection experiment — user pause handoff

PAUSED_BY_USER (low battery). All experiment processes stopped with SIGKILL after stopping orchestrators; no new runs permitted until user resumes. Evidence root: `/home/mint/ws_fusion_uwb/res/nlos_injection_20260911_01`; authoritative pause ledger `PAUSED.json`.

Baseline HEAD46d37f6709aeedb4958e484683fd51e5fd8ec0cc; only protected preexisting untracked dir was doc/v2/ie_0911/. Do not commit/push. Baseline archive/hash exists. Source/config algorithms frozen except authorized cache reader IO; runner adds FDE-only stop/config truth guard. New files: export_sfuise_input.cpp, nlos_injection.py, nlos_injection_metrics.py, nlos_experiment_common.py, run_nlos_injection_experiment.py, evaluate_nlos_clean_gate.py, report_nlos_injection.py, test_nlos_injection.py. Build passed; CTest30/30 twice (128.81s/140s); latest focused Python6 tests passed. Interface tests and bwrap+strace truth isolation passed. Current binary is fully built; no compilation pending.

Selection and clean gates locked in locked_manifest.json: 6 clean +4 admitted injected, 2 skipped injected (Walk1 low and Walk2 normal: Cauchy clean solver failure). All 3 selected windows exactly8s; B=.5, seed911. normal links/windows and low subsets in manifest. selection_inventory.json expands complete read-only endpoint-gap/subset inventory. Small endpoint eligibility implementation correction was checked against all chosen normal windows and attempted low eligibility: unchanged; no reselection/estimator retry. Latest generator regenerates all10 input caches byte-identically (engineering/locked_manifest_regeneration.json).

Completed E2E batches: Walk1 normal clean/injected, Walk1 low clean; Walk2 normal clean, low clean/injected; Walk3 normal clean. Walk3 normal injected raw/Cauchy completed; its shared producer interrupted in run t09-c4b912ef0533459ea6b544b8f8333199, four finals not run. Walk3 low clean/injected E2E not started (their raw/Cauchy clean gate baselines already complete). Consult PAUSED.json for precise states. Walk1 normal injected FDE was SUCCESS_EMPTY, zero faults/support; Walk2 low injected Cauchy failed, its producer/finals completed. Do not infer recovery benefit from empty support.

IMPORTANT RESUME: current top-level resume implementation skips an entire batch if .log.command.json exists. Do NOT run it blindly: it would seal incomplete interrupted batch and miscount missing finals. Need resume only unattempted cells; preserve interrupted producer as INTERRUPTED_USER_PAUSE (not algorithm failure). Original plan disallows algorithm retries; do not restart interrupted optimization without interpreting/obtaining explicit user resume authorization. Independent remaining Walk3 low batches can proceed once resumed. Existing scheduler supports cache producer references—inspect CLI/manifest before resuming cells. Do not repeat completed raw/Cauchy/producer/finals. Preserve old batch manifest backup and run status backup.

Reports NOT_RUN: final independent report must only read truth after all intended estimator outputs sealed. report_nlos_injection.py guard requires each nonskipped scenario e2e_batch in execution_manifest.json; missing/interrupted rows need explicit accounting on resume. It generates doc/ie_0911/NLOS_INJECTION_EXPERIMENT_RESULT.md and report/results.json, table_a.csv, table_b.csv, low pairs, commands. Script not yet run end-to-end; verify syntax/real schema, complete metrics/table audit. It enriches eval-only truth sidecar planned IDs. No final scientific judgment yet. Desired final judgments may be negative/insufficient.

Remaining review: ensure trajectory coverage checks exact planned timestamps (currently count based; original runner emits all keyframes), complete per-metric denominators, retain failure/fallback and no-source range UNAVAILABLE. New metrics include weighted injected support component, unavailable FDE partial checks, correction final-use/fallback separation. Latest focused tests have6 cases, fullCTest30/30 before some added edge assertions, with later focused tests passing. Do not claim full matrix complete. Full final report command/exit ledger must include actual build commands and initial engineering failures (origin YAML precision, first target config, provenance whitelist), not only *.command.json files. Archive final source/binary/dependency hashes, check frozen materials/algorithms unchanged, update STATUS/EXPERIMENT_CONTRACT/protocol/CLAIM_EVIDENCE and stop.
